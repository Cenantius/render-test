{
  "name": "puhelinluettelo",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "iisakki",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "@powershell Remove-Item -Recurse -Force dist && cd ../../osa2/puhelinluettelo && npm run build && @powershell Copy-Item dist -Recurse ../../osa2/puhelinluettelo",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "express": "^5.1.0",
    "morgan": "^1.10.1"
  }
}
